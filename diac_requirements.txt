# Arabic Diacritics Editor — Python dependencies
# Freeze after all Phase 1 tests pass:
#   pip freeze > requirements.txt
# Then commit the pinned file.

flask
regex

# Testing only — not required for running the app
pytest
